Secrets Management Strategy
Secrets are stored inside Kubernetes as sealed Secret objects mounted into pods via environment variable bindings (valueFrom.secretKeyRef).

Secrets are never committed to Git or Helm chart values. The templates/secret-example.yaml file exists only as a non-production schema reference.

Production systems should integrate AWS Secrets Manager synced to the cluster via the External Secrets Operator (ESO) or HashiCorp Vault.